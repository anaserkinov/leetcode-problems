#include <iostream>
#include <string>

using namespace std;

class Solution {
 public:
    string convert(string s, int numRows) {
        
    }
};

int main() {
  string input;
  int rows;
  cin >> input;
  cin>>rows;

  Solution s;

  cout << s.convert(input, rows);

  return 0;
}